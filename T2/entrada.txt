constante PI: real = 3.14

procedimento imprime_msg(msg: literal)
	escreva(msg)
fim_procedimento

funcao soma(a, b: inteiro): inteiro
	retorne a + b
fim_funcao

algoritmo
	declare x, y: inteiro
	declare r: real
	declare ok: logico
	declare nome: literal

	leia(x, y)
	r <- soma(x, y)
	ok <- verdadeiro
	nome <- "teste"

	se x > y entao
		escreva("x maior")
	senao
		escreva("y maior")
	fim_se

	para x <- 1 ate 3 faca
		escreva(x)
	fim_para

	enquanto x < 10 faca
		x <- x + 1
	fim_enquanto

	faca
		x <- x - 1
	ate x = 0

	caso x seja
		0: escreva("zero")
		1..3: escreva("baixo")
	senao
		escreva("outro")
	fim_caso

	imprime_msg(nome)
fim_algoritmo
